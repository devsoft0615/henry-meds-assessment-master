import { Layout } from "antd";

const { Header } = Layout;

export const AppHeader = () => {
  return (
    <Header style={{ textAlign: "center", color: "white", fontSize: 24 }}>
      Henry Meds Assessment
    </Header>
  );
};
